TimerInitTime = 300;
num_pcbs = 6;


initialize() {
  for(int i; i < num_pcbs; i++){
      // creating the pcbs
      pcb_p pcb = create_pcb(i, ,);
      // assign MAX_PC
      // assign Terminate
      // assign IO Trap Arrays
  }
}